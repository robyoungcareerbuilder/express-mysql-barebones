# express-mysql-barebones

1. Create a MySQL db.
2. Update these lines in server.js with your MySQL connection data:
    var con=mysql.createConnection({
        host: "localhost",
        database: "nodejs",
        user: "robsdb",
        password: ""
    })
3. run "npm install"
4. run "node server.js"

